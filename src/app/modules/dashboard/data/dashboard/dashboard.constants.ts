const DASHBOARD = '[DASHBOARD]';

export const DASHBOARD_REQUESTED = `${DASHBOARD} REQUESTED`;
export const DASHBOARD_SUCCEEDED = `${DASHBOARD} SUCCEEDED`;
export const DASHBOARD_FAILED = `${DASHBOARD} FAILED`;
